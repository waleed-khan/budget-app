{
  "name": "budget-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo \"No dev command configured yet. Scaffold apps/web and apps/api first.\"",
    "build": "echo \"No build command configured yet.\"",
    "lint": "echo \"No lint command configured yet.\""
  }
}
